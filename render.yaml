# Render blueprint: creates the TR-1 web service automatically
# when you pick "New +" -> "Blueprint" and select this repo.
services:
  - type: web
    name: tr1-routine-tracker
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
